🎯 **What:** The testing gap addressed is the lack of unit tests for the UI API client (`ui/src/lib/api.ts`).
📊 **Coverage:** Mocked `fetch` and `localStorage` to cover scenarios for auth header assignment, JSON parsing across helpers like `getJson`, `sendJson`, and `getAnalytics`, as well as control plane error throwing, and HTTP status handling (404/503 fallback mechanisms).
✨ **Result:** Core UI data-fetching paths and helper mechanisms are now verified and regression-tested, enabling safe refactoring of the frontend network layer.
